# CasaOS Development Environment Configuration
# Copy this file to .env and fill in your values

# Required: Your domain for CasaOS (e.g., mynas.nsl.sh or nas.localhost)
PCS_DOMAIN=

# Required: Default password for CasaOS admin user
PCS_DEFAULT_PASSWORD=

# Required: Your email address (used for notifications and SSL certificates)
PCS_EMAIL=

# Required: Public IP address of your server (use 127.0.0.1 for local dev)
PUBLIC_IP=

# Optional: Host path for data storage (defaults to /DATA)
# Must be same as Container internal data root (defaults to /DATA)
# On Windows with Docker Desktop, use /c/DATA or similar
DATA_HOST_PATH=/DATA
