# Environment variables for FileBrowser
# These are provided by the platform at runtime (injected in docker compose up)

# Data root directory (where app data is stored)
APP_DATA_ROOT=/DATA

# Domain for routing (without https://)
APP_DOMAIN=example.nsl.sh

# Network name for container connectivity
APP_NET=pcs

# Default password for app initialization
APP_DEFAULT_PASSWORD=changeme

# Public IP addresses
APP_PUBLIC_IP=1.2.3.4
APP_PUBLIC_IPV6=::1

# Email for notifications/certs
APP_EMAIL=admin@example.nsl.sh

# User/Group IDs for file permissions (standard Linux)
PUID=1000
PGID=1000

# Timezone (standard)
TZ=UTC
